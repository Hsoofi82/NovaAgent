{
  "name": "ƝØVΛᵅᵍᵉⁿᵗ",
  "version": "1.0.0",
  "description": "Nova AI Telegram Bot on Cloudflare Workers",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test tests/regression.test.mjs tests/core.test.mjs && npm run test:engines",
    "type-check": "tsc --noEmit",
    "validate": "npm run type-check && npm test && wrangler deploy --dry-run --outdir=.wrangler/dry",
    "cf-types": "wrangler types",
    "test:engines": "node --experimental-loader ./tests/ts-resolve.mjs --test tests/engines.test.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "wrangler": "^4.112.0",
    "typescript": "^5.7.2"
  }
}
